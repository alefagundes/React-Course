const ExecutingFunction = ({ myFunction }) => {
    return (
        <div>
            <button onClick={myFunction}>
                Clique para ver a execucao da funcao
            </button>
        </div>
    )
}

export default ExecutingFunction
