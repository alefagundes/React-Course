const name = 'alessandro'
const age = 21

//recurso disponivel com a vinda do ES6 para impressao de textos de maneira mais otimizada para o dev
//constituise em ``(crase) e passar a variavel com ${} ex abaixo:

console.log(`${name} tem ${age}`)
